//JAVASCRIPT: w3schools. Es el lenguaje mas popular y segun dice es uno de los mas faciles de aprender.

//variables: var, let, const (constante)
//las variables van cambiando a lo largo de la ejecucion del programa, las const no van a cambiar y se le asigna el valor cuando se la declara (ej: const "IVA = 21")

//var se utilizo hasta 2015, se puede seguir usando pero ahora se usa let y const

//Las variables tienen que tener nombres que sean identificadores unicos, por lo que hay que seguir una buena practica para identificarlos, ponerles nombres que los representen
//Los nombres de las variables tienen que empezar con una letra, signo dolar o guion bajo.
//Es importante tener cuidado con el case sensitive porque no es lo mismo mayusculas que minusculas
//No se pueden usar palabras reservadas (var, let, etc.) para definir o declarar variables 


//El javascript sirve para agregar interactividad a la pagina, es para que el usuario ejecute los eventos que nosotros programamos
/**
 * Donde se situa el javascript en un documento HTML?
 * Pueden encerrarse dentro de los elementos script (<script>...</script>)
 * Eso esta bien si es que la pagina esta formada por un solo documento HTML.
 * Si no, se hace de otra manera
 */

/**
 * Integrando el js al documento html:
 * Cuando hago un documento js aparte del html, lo que voy a hacer para integrarlo es usar el elemento script en el html(en el <body>), pero como las img le voy a poner src = "direccion del documento js" y cierro con otra script, entonces ya quedaria linkeado 
 */